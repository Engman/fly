
Texture2D colorSRV : register(t0);
Texture2D LightSRV : register(t1);

SamplerState SampleTypeClamp 	:	 register(s0);



//-----------------------------------------------------------------------------------------
// Input and Output Structures
//-----------------------------------------------------------------------------------------

struct PSIn
{
	float4 Pos			: SV_Position;
	float2 TextCoord  	: TEXCOORD; 
};

//-----------------------------------------------------------------------------------------
// PixelShader: PSSceneMain
//-----------------------------------------------------------------------------------------
float4 FPixelShader(PSIn input) :  SV_Target
{	
	//textCoord = float2(0.5f*input.Pos.x, -0.5f*input.Pos.y) + 0.5f;
	// float4 color = colorSRV.Sample( SampleTypeClamp, input.TextCoord); //textCoord );
	//return color;
  
	//sample from the lighted scene texture
	float4 lighting = LightSRV.Sample( SampleTypeClamp, input.TextCoord);
 
  //float4 finalColor = float4(color.xyz * lighting.xyz + lighting.w, 1);
  float4 finalColor =lighting;
  return finalColor;
}

